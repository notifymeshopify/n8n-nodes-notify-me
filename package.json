{
  "name": "n8n-nodes-notify-me",
  "version": "1.0.3",
  "description": "n8n community node for Notify Me! — Shopify back-in-stock, pre-order, and waitlist notifications.",
  "keywords": [
    "n8n-community-node-package",
    "n8n",
    "notify-me",
    "shopify",
    "back-in-stock",
    "pre-order",
    "waitlist",
    "restock"
  ],
  "license": "MIT",
  "homepage": "https://notify-me.io",
  "author": {
    "name": "Notify Me!",
    "email": "support@notify-me.io",
    "url": "https://notify-me.io"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/notifymeshopify/n8n-nodes-notify-me.git"
  },
  "bugs": {
    "url": "https://github.com/notifymeshopify/n8n-nodes-notify-me/issues"
  },
  "engines": {
    "node": ">=20.15"
  },
  "main": "index.js",
  "scripts": {
    "build": "npx rimraf dist && tsc && gulp build:icons",
    "dev": "tsc --watch",
    "format": "prettier nodes credentials --write",
    "lint": "eslint nodes credentials",
    "lintfix": "eslint nodes credentials --fix",
    "prepublishOnly": "npm run build && npm run lint -c .eslintrc.prepublish.js nodes credentials"
  },
  "files": [
    "dist"
  ],
  "n8n": {
    "n8nNodesApiVersion": 1,
    "credentials": [
      "dist/credentials/NotifyMeApi.credentials.js"
    ],
    "nodes": [
      "dist/nodes/NotifyMe/NotifyMe.node.js",
      "dist/nodes/NotifyMeTrigger/NotifyMeTrigger.node.js"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/parser": "~8.32.0",
    "eslint": "^8.57.0",
    "eslint-plugin-n8n-nodes-base": "^1.16.3",
    "gulp": "^5.0.0",
    "n8n-workflow": "*",
    "prettier": "^3.5.3",
    "rimraf": "^5.0.5",
    "typescript": "^5.8.2"
  },
  "peerDependencies": {
    "n8n-workflow": "*"
  }
}
